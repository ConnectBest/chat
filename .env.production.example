# Production Environment Configuration for Frontend
# Copy this to .env.local in production

# NextAuth Configuration
# REQUIRED: Set to your production domain
NEXTAUTH_URL=https://yourdomain.com
# REQUIRED: Generate a strong random secret (use: openssl rand -base64 32)
NEXTAUTH_SECRET=CHANGE-THIS-TO-STRONG-RANDOM-SECRET-KEY-IN-PRODUCTION

# Backend API Configuration
# REQUIRED: Set to your production backend URL
NEXT_PUBLIC_API_URL=https://api.yourdomain.com/api
NEXT_PUBLIC_WS_URL=https://api.yourdomain.com

# Google OAuth Credentials (if using)
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-production-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-production-google-client-secret

# Email Configuration (for 2FA verification emails)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-production-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password
EMAIL_FROM=noreply@yourdomain.com

# Environment
NODE_ENV=production

# Production Notes:
# 1. Never commit .env.local to version control
# 2. Set environment variables in your hosting platform (Vercel, Netlify, etc.)
# 3. For NEXTAUTH_SECRET, run: openssl rand -base64 32
# 4. Ensure CORS_ORIGINS in backend includes your frontend domain
# 5. Use HTTPS in production for security
# 6. Keep all secrets secure and rotate them regularly
