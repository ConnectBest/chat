# 🏗️ Architecture & Data Flow Diagrams

## System Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                        FRONTEND                               │
│                     (Next.js/React)                           │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐       │
│  │  Login  │  │Channels │  │Messages │  │ Profile │       │
│  │  Pages  │  │  Views  │  │  Views  │  │  Pages  │       │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘       │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        │ HTTP/REST API (JSON)
                        │ Authorization: Bearer <JWT>
                        ▼
┌──────────────────────────────────────────────────────────────┐
│                        BACKEND                                │
│                      (Python Flask)                           │
│  ┌────────────────────────────────────────────────────────┐  │
│  │              API Routes (Controllers)                   │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐           │  │
│  │  │   Auth   │  │ Channels │  │ Messages │           │  │
│  │  │  Routes  │  │  Routes  │  │  Routes  │           │  │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘           │  │
│  └───────┼─────────────┼──────────────┼─────────────────┘  │
│          │             │              │                     │
│  ┌───────▼─────────────▼──────────────▼─────────────────┐  │
│  │              Utils & Middleware                       │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐          │  │
│  │  │   JWT    │  │   Input  │  │   CORS   │          │  │
│  │  │  Auth    │  │Validation│  │  Config  │          │  │
│  │  └──────────┘  └──────────┘  └──────────┘          │  │
│  └──────────────────────┬────────────────────────────────┘  │
│                         │                                    │
│  ┌──────────────────────▼────────────────────────────────┐  │
│  │                   Models                              │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐          │  │
│  │  │   User   │  │ Channel  │  │ Message  │          │  │
│  │  │  Model   │  │  Model   │  │  Model   │          │  │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘          │  │
│  └───────┼─────────────┼──────────────┼─────────────────┘  │
└──────────┼─────────────┼──────────────┼────────────────────┘
           │             │              │
           └─────────────┴──────────────┘
                        │
                        │ PyMongo
                        ▼
┌──────────────────────────────────────────────────────────────┐
│                       DATABASE                                │
│                       (MongoDB)                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │  users   │  │ channels │  │ messages │  │ channel_ │   │
│  │collection│  │collection│  │collection│  │ members  │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└──────────────────────────────────────────────────────────────┘
```

---

## Authentication Flow

```
┌─────────┐                                          ┌─────────┐
│ Client  │                                          │ Server  │
│(Browser)│                                          │ (Flask) │
└────┬────┘                                          └────┬────┘
     │                                                    │
     │ 1. POST /api/auth/register                        │
     │    {email, password, name}                        │
     ├──────────────────────────────────────────────────►│
     │                                                    │
     │                                    2. Validate input
     │                                    3. Hash password (bcrypt)
     │                                    4. Store in MongoDB
     │                                    5. Generate JWT token
     │                                                    │
     │ 6. Response: {user, token}                        │
     │◄──────────────────────────────────────────────────┤
     │                                                    │
     │ 7. Store token in localStorage                    │
     │                                                    │
     │ 8. GET /api/chat/channels                         │
     │    Header: Authorization: Bearer <token>          │
     ├──────────────────────────────────────────────────►│
     │                                                    │
     │                                    9. Verify JWT token
     │                                    10. Decode user info
     │                                    11. Check permissions
     │                                    12. Query MongoDB
     │                                                    │
     │ 13. Response: {channels: [...]}                   │
     │◄──────────────────────────────────────────────────┤
     │                                                    │
```

**Key Points:**
- **Never send password in plain text after initial login**
- **Token is signed and verifiable**
- **Token contains user ID, email, role**
- **Token has expiration date**

---

## Message Sending Flow

```
┌───────┐      ┌─────────┐      ┌────────┐      ┌──────────┐
│ User  │──────│ Routes  │──────│ Models │──────│ MongoDB  │
└───┬───┘      └────┬────┘      └───┬────┘      └────┬─────┘
    │               │               │                │
    │ Send Message  │               │                │
    ├──────────────►│               │                │
    │               │               │                │
    │               │ 1. Verify JWT │                │
    │               │    token      │                │
    │               │               │                │
    │               │ 2. Check user │                │
    │               │    is member  ├───────────────►│
    │               │               │◄───────────────┤
    │               │               │                │
    │               │ 3. Validate   │                │
    │               │    content    │                │
    │               │               │                │
    │               │ 4. Create     │                │
    │               │    message    │                │
    │               ├──────────────►│                │
    │               │               │                │
    │               │               │ 5. Insert      │
    │               │               │    document    │
    │               │               ├───────────────►│
    │               │               │                │
    │               │               │ 6. Return ID   │
    │               │               │◄───────────────┤
    │               │               │                │
    │               │ 7. Return     │                │
    │               │    message    │                │
    │◄──────────────┤               │                │
    │               │               │                │
    │ Show message  │               │                │
    │               │               │                │
```

---

## Database Schema Relationships

```
┌─────────────────────────────────────────────────────────┐
│                         USERS                           │
│  _id, email, password_hash, name, role, status         │
└──────────────┬──────────────────────────┬───────────────┘
               │                          │
               │ created_by               │ user_id
               │ (one-to-many)            │ (one-to-many)
               │                          │
               ▼                          ▼
┌──────────────────────────┐    ┌─────────────────────────┐
│       CHANNELS           │    │  CHANNEL_MEMBERS        │
│  _id, name, type,        │◄───│  channel_id, user_id,   │
│  description,            │    │  role, joined_at        │
│  created_by              │    │                         │
└──────────────┬───────────┘    └─────────────────────────┘
               │                          ▲
               │ channel_id               │ (junction table)
               │ (one-to-many)            │ (many-to-many)
               │                          │
               ▼                          │
┌──────────────────────────┐    ┌─────────────────────────┐
│       MESSAGES           │    │                         │
│  _id, channel_id,        │    │  Represents:            │
│  user_id, content,       │    │  Users ←→ Channels      │
│  parent_message_id,      │    │  (many-to-many)         │
│  is_edited, is_pinned    │    │                         │
└──────────────┬───────────┘    └─────────────────────────┘
               │
               │ message_id
               │ (one-to-many)
               │
               ▼
┌──────────────────────────┐
│   MESSAGE_REACTIONS      │
│  message_id, user_id,    │
│  emoji, created_at       │
└──────────────────────────┘
```

**Relationships:**
- **User → Channels**: One user can create many channels
- **User ↔ Channels**: Many-to-many (via channel_members)
- **Channel → Messages**: One channel has many messages
- **User → Messages**: One user sends many messages
- **Message → Reactions**: One message has many reactions

---

## Request/Response Cycle

```
┌──────────────────────────────────────────────────────────────┐
│                    REQUEST LIFECYCLE                          │
└──────────────────────────────────────────────────────────────┘

1. HTTP Request arrives
   └─► http://localhost:5000/api/chat/channels
       Method: GET
       Headers: Authorization: Bearer eyJ...

2. Flask receives request
   └─► app.py → Flask application

3. CORS Middleware
   └─► Check Origin header
   └─► Add CORS headers to response

4. Route Matching
   └─► Find matching route in channels.py
   └─► GET /api/chat/channels → ChannelList.get()

5. Authentication Check
   └─► Extract JWT from Authorization header
   └─► Verify token signature
   └─► Decode user info from payload
   └─► Check expiration

6. Controller Logic
   └─► Get user_id from token
   └─► Call channel model methods

7. Model/Database
   └─► Build MongoDB query
   └─► Execute aggregation pipeline
   └─► Fetch channel documents

8. Response Formatting
   └─► Convert ObjectIds to strings
   └─► Format timestamps
   └─► Build JSON response

9. HTTP Response
   └─► Status: 200 OK
   └─► Content-Type: application/json
   └─► Body: {channels: [...]}

10. Client receives response
    └─► Parse JSON
    └─► Update UI
```

---

## JWT Token Structure

```
┌────────────────────────────────────────────────────────────┐
│                    JWT TOKEN STRUCTURE                      │
└────────────────────────────────────────────────────────────┘

Full Token:
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyX2lkIjoiNjVhYmMxMjMi...

Decoded Structure:

┌─────────────┐   ┌──────────────────┐   ┌─────────────────┐
│   HEADER    │ . │     PAYLOAD      │ . │   SIGNATURE     │
└─────────────┘   └──────────────────┘   └─────────────────┘
      │                    │                       │
      │                    │                       │
      ▼                    ▼                       ▼
{                    {                        HMACSHA256(
  "alg": "HS256",      "user_id": "65abc",    base64UrlEncode(header) +
  "typ": "JWT"         "email": "user@...",   "." +
}                      "role": "user",        base64UrlEncode(payload),
                       "iat": 1704902400,     secret
                       "exp": 1705507200    )
                     }

┌────────────────────────────────────────────────────────────┐
│  HEADER      │  Algorithm & token type                     │
│  PAYLOAD     │  Data (user info, expiration)              │
│  SIGNATURE   │  Verification (ensures not tampered)       │
└────────────────────────────────────────────────────────────┘

**Why JWT?**
- ✅ Stateless (no server-side session storage)
- ✅ Scalable (works across multiple servers)
- ✅ Self-contained (has user info)
- ✅ Secure (cryptographically signed)
- ✅ Standard (works with many libraries)
```

---

## Error Handling Flow

```
┌──────────────────────────────────────────────────────────────┐
│                     ERROR SCENARIOS                           │
└──────────────────────────────────────────────────────────────┘

Scenario 1: Invalid Input
────────────────────────────
Request: POST /api/auth/register
Body: {email: "invalid", password: "short"}
    │
    ├─► Validation Layer
    │   └─► Email format check ❌
    │   └─► Password strength check ❌
    │
    └─► Response: 400 Bad Request
        {error: "Invalid email format"}


Scenario 2: Authentication Error
────────────────────────────────
Request: POST /api/auth/login
Body: {email: "user@example.com", password: "wrong"}
    │
    ├─► Find user in database ✓
    ├─► Verify password ❌
    │
    └─► Response: 401 Unauthorized
        {error: "Invalid credentials"}


Scenario 3: Authorization Error
───────────────────────────────
Request: DELETE /api/messages/123
Header: Authorization: Bearer <valid-token>
    │
    ├─► Verify JWT ✓
    ├─► Find message ✓
    ├─► Check ownership ❌ (not message author)
    │
    └─► Response: 403 Forbidden
        {error: "Not authorized to delete this message"}


Scenario 4: Not Found
──────────────────────
Request: GET /api/chat/channels/nonexistent-id
    │
    ├─► Verify JWT ✓
    ├─► Query database
    ├─► No document found ❌
    │
    └─► Response: 404 Not Found
        {error: "Channel not found"}


Scenario 5: Server Error
─────────────────────────
Request: Any endpoint
    │
    ├─► Database connection fails ❌
    │   OR unexpected exception
    │
    └─► Response: 500 Internal Server Error
        {error: "An unexpected error occurred"}
```

---

## MongoDB Query Examples

```
┌────────────────────────────────────────────────────────────┐
│                    MONGODB OPERATIONS                       │
└────────────────────────────────────────────────────────────┘

1. Insert User
──────────────
db.users.insertOne({
  email: "john@example.com",
  password_hash: "$2b$12$...",
  name: "John Doe",
  role: "user",
  created_at: ISODate("2024-01-15T10:30:00Z")
})

Result: { insertedId: ObjectId("65abc123...") }


2. Find User by Email
─────────────────────
db.users.findOne({
  email: "john@example.com"
})

Result: { _id: ObjectId("..."), email: "...", ... }


3. List User's Channels (Aggregation)
──────────────────────────────────────
db.channel_members.aggregate([
  // Stage 1: Find user's memberships
  { $match: { user_id: ObjectId("65abc...") } },
  
  // Stage 2: Join with channels collection
  {
    $lookup: {
      from: "channels",
      localField: "channel_id",
      foreignField: "_id",
      as: "channel"
    }
  },
  
  // Stage 3: Flatten array
  { $unwind: "$channel" },
  
  // Stage 4: Sort by join date
  { $sort: { joined_at: -1 } }
])


4. Get Messages with User Info (Aggregation)
─────────────────────────────────────────────
db.messages.aggregate([
  // Stage 1: Filter by channel
  {
    $match: {
      channel_id: ObjectId("65abc..."),
      is_deleted: false
    }
  },
  
  // Stage 2: Sort newest first
  { $sort: { created_at: -1 } },
  
  // Stage 3: Limit results
  { $limit: 50 },
  
  // Stage 4: Join with users
  {
    $lookup: {
      from: "users",
      localField: "user_id",
      foreignField: "_id",
      as: "user"
    }
  },
  
  // Stage 5: Flatten user array
  { $unwind: "$user" }
])


5. Update Message
─────────────────
db.messages.updateOne(
  { _id: ObjectId("65abc...") },
  {
    $set: {
      content: "Updated content",
      is_edited: true,
      edited_at: ISODate("2024-01-15T11:00:00Z")
    }
  }
)


6. Delete Message (Soft Delete)
───────────────────────────────
db.messages.updateOne(
  { _id: ObjectId("65abc...") },
  {
    $set: {
      is_deleted: true,
      deleted_at: ISODate("2024-01-15T12:00:00Z")
    }
  }
)
```

---

## Performance Considerations

```
┌────────────────────────────────────────────────────────────┐
│              OPTIMIZATION STRATEGIES                        │
└────────────────────────────────────────────────────────────┘

1. Database Indexes
───────────────────
users.createIndex({email: 1})          // Unique, fast lookups
channels.createIndex({name: 1})        // Unique, fast lookups
messages.createIndex({channel_id: 1, created_at: -1})  // Composite


2. Query Optimization
─────────────────────
✅ Use projection to limit returned fields
✅ Use cursor-based pagination (not skip/limit)
✅ Avoid $where operations
✅ Use covered queries when possible


3. Connection Pooling
──────────────────────
PyMongo automatically manages connection pool
Configure: maxPoolSize, minPoolSize


4. Caching Strategy (Future)
────────────────────────────
Redis for:
- User sessions
- Frequently accessed data
- Rate limiting
- Real-time presence


5. N+1 Query Prevention
───────────────────────
Use aggregation pipelines with $lookup
Instead of: For each message, query user
Do: One aggregation with join
```

---

This architecture provides a solid foundation that can scale as your application grows!
